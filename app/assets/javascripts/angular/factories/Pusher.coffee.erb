angular
  .module("Poll")
  .factory "Pusher", ->

    Pusher.host = "<%= ENV['PUSHER_CLIENT_HOST'] %>"
    Pusher.ws_port = <%= ENV['PUSHER_CLIENT_PORT'] %>
    Pusher.wss_port = <%= ENV['PUSHER_CLIENT_PORT_SSL'] %>

    new Pusher("<%= ENV['PUSHER_KEY'] %>")
